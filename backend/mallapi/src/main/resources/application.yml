spring:
    application:
        name: mallapi
    datasource:
        url: jdbc:mariadb://localhost:3326/malldb
        username: malldbuser
        password: malldbuser
        driver-class-name: org.mariadb.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update